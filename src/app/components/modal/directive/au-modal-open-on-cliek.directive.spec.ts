import { AuModalOpenOnCliekDirective } from './au-modal-open-on-cliek.directive';

describe('AuModalOpenOnCliekDirective', () => {
  it('should create an instance', () => {
    const directive = new AuModalOpenOnCliekDirective();
    expect(directive).toBeTruthy();
  });
});
